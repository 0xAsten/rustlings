// variables3.rs
//
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a
// hint.

// Note: the value of a variable must be initialized
fn main() {
    let x: i32 = 0;
    println!("Number {}", x);
}
